<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.FlashcardsApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="android:textColorPrimary">@color/dark_text</item>
        <item name="android:colorPrimary">@color/teal_200</item>
        <item name="android:colorAccent">@color/teal_700</item>
        <item name="android:toolbarStyle">@style/ToolbarStyle</item>
        <item name="colorControlNormal">@color/dark_background</item> <!-- Menu icon color -->

    </style>

    <style name="Theme.FlashcardsApp" parent="Base.Theme.FlashcardsApp" />

    <!-- Toolbar Style -->
    <style name="ToolbarStyle" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/black</item>
        <item name="android:titleTextColor">@color/white</item>
    </style>
</resources>
